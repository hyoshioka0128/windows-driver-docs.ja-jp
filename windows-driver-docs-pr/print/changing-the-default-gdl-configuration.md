---
title: 既定の GDL 構成の変更
description: 既定の GDL 構成の変更
ms.assetid: ecc4a6ab-869a-402e-b90e-5ad94e0347c3
keywords:
- GDL WDK の構成
- WDK GDL、既定の構成の構成
- WDK GDL、既定の構成の変更の構成
- WDK の既定の GDL 構成
- DefaultOption ディレクティブ WDK GDL
ms.date: 04/20/2017
ms.localizationpriority: medium
ms.openlocfilehash: e55c695942305c97d2748d5a39599897697e84d4
ms.sourcegitcommit: 0cc5051945559a242d941a6f2799d161d8eba2a7
ms.translationtype: MT
ms.contentlocale: ja-JP
ms.lasthandoff: 04/23/2019
ms.locfileid: "63347248"
---
# <a name="changing-the-default-gdl-configuration"></a>既定の GDL 構成の変更


 **\*DefaultOption**ディレクティブ自体が、構成によって異なります。 異なる既定の構成を定義するには、ディレクティブ内で複数回を定義することで、 **\*スイッチ**と**\*ケース**を構築します。 依存関係と競合していないことを確認する必要があります、ただし、  **\*ConflictPriority**パラメーターごとに確立されました。

必要がありますを開始する既定の構成で、安全であるパラメーターの値の一部を明示的に設定しようとしている場合でもです。 完全な構成にはでの対応していないこと、およびそのパラメーターが含まれますが指定されていないかどうかは、ゼロから独自の構成を作成しようとしました。 します。 また、GDL ファイルでは、設定することを計画していたいくつかのパラメーターが定義されていません可能性があります。

たとえば、クライアントが既定構成を取得しを新しい値に 2 つのパラメーターを更新します。 2 つのパラメーターは、今日の天気場合、クライアントは、date 関数を照会し、今日が金曜日であることを検出します。 クライアント、インターネットからの現在の天気をチェックし、天気が Sunny であることを検出)。

最初に、クライアント確認してくださいは既定のスナップショットを調べることで、今日の天気のパラメーターが GDL ファイルで定義されています。 クライアントはことを確認し、金曜日晴れた日の値が GDL ファイルに定義されています。 既定のスナップショットを検索する DOM メソッドを使用して、これらの値を確認できます。 この検証では、クライアントは、各構成でこれらのパラメーターの既定値を保持しているノードを見つけます、新しい値に更新しています。

それ以外の場合、構成はユーザー入力から取得または永続的なストレージから取得されます。 クライアントでは、スナップショットを取得するのにこれらの構成も使用できます。

 

 




