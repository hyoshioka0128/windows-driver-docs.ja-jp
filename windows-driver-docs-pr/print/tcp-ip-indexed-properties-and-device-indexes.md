---
title: TCP/IP のプロパティとデバイスのインデックスをインデックス作成
description: TCP/IP のプロパティとデバイスのインデックスをインデックス作成
ms.assetid: b26b0c18-1787-43e0-8461-acfbd9fb38f9
keywords:
- インデックス付きプロパティの WDK プリンターの TCP/IP
- インデックス付きプロパティの WDK プリンター
- デバイスのインデックスの WDK プリンター
ms.date: 04/20/2017
ms.localizationpriority: medium
ms.openlocfilehash: debaae0b9b7525edd897c6cb364263f98ea209e9
ms.sourcegitcommit: a33b7978e22d5bb9f65ca7056f955319049a2e4c
ms.translationtype: MT
ms.contentlocale: ja-JP
ms.lasthandoff: 01/31/2019
ms.locfileid: "56551750"
---
# <a name="tcpip-indexed-properties-and-device-indexes"></a>TCP/IP のプロパティとデバイスのインデックスをインデックス作成


インデックス付きのプロパティは、により、同じ名前を共有する複数の関連するプロパティ スキーマのプロパティ名がそれぞれが個別のプロパティを識別する数値インデックスに追加される数値インデックスを使用できます。 インデックスの値は正の整数である必要がありますが、そのサイズの上限はありません。 スキーマ クエリでは、特定の要素に関連付けられている必要のあるインデックス値を決定します。 このメカニズムでは、MIB テーブル内のデータにアクセスできます。

IndexedProperty コンス トラクターは、インデックス付きプロパティを定義します。 このコンス トラクターに次の制限が適用されます。

-   IndexedProperty のコンス トラクターは、プロパティのコンス トラクターの親にすることはできません。 NonIndexedProperty コンストラクトとは、IndexedProperty コンストラクトでインデックスがない場合のプロパティを定義する 1 つ。

-   IndexedProperty コンストラクトも NonIndexedProperty コンス トラクターをインストール済みの構成要素の親となることができます。

クエリは、別のインデックスもあります。 デバイスのインデックス。 SNMP をサポートするネットワーク デバイスは、サブデバイス、プリンターなどのさまざまなホストであることができます。 ネットワーク プリンターの MIB テーブルでは、デバイスの種類 (デバイスのインデックス) に基づいてインデックスが作成されるエントリがあります。 MIB テーブルからデータを取得するクエリの順序は、クエリによって、正しいデバイス インデックスする必要があります。 標準の TCP/IP ポート モニタでは、ポートの構成 UI で構成できるようにデバイスのインデックスをできます。 Bidi 拡張子**deviceIndex**に設定されている**TRUE** (を参照してください、[値](value.md)と[インストール済み](installed2.md)を構築します) を生成する OID と、デバイスのインデックスを持つ元の OID を連結します。 インデックス付きプロパティのインデックスは、使用されている場合、次のデバイスのインデックス、OID に連結されます。

### <a name="code-example"></a>コード例

次のコード例では、TCP/IP 双方向通信のスキーマを拡張を追加して、**表示**プロパティを**プリンター**プロパティ。 さらに、**表示**プロパティがインデックス付きプロパティ、**行**、あり**deviceIndex**に設定**TRUE**します。 ここで示すように、スキーマでは、特定のプリンターの表示の行からテキストを取得するクエリを生成します。

```cpp
<Property name="Printer">
  <Property name="Display">
    <IndexedProperty name="Row">
      <Value name="Text" type="BIDI_TEXT" 
             oid="1.3.6.1.2.1.43.16.5.1.2" deviceIndex="true"/>
    </IndexedProperty>
  </Property>
</Property>
```

前の例は、次のクエリ結果します。

```cpp
\Printer.Display.Row1:Text
```

この例から生成される OID は、まずと同じ、 **oid**属性、**値**プロパティが、2 つのインデックスが追加されます。 例で追加されたインデックスに起因、 **deviceIndex**属性に設定されている**TRUE**と**行**されるインデックス付きプロパティ。 ポートの構成 UI が 111、インデックスにデバイスを定義し、目的のプリンターの表示の 1 行のテキストであると仮定に生成される OID は 1.3.6.1.2.1.43.16.5.1.2.111.1 になります。 この OID は、デバイスのインデックス (111) と最後にプロパティのインデックス (1) を除き、元と同じです。 場合**deviceIndex**に設定されている必要がある**FALSE**または結果として得られる OID により、1.3.6.1.2.1.43.16.5.1.2.1 いたでしょうし、省略されていますがありました。 行のテキストを表示する*n* 、ディスプレイのプロパティのインデックスを使用して、 *n*します。

 

 




