---
title: 構成依存データのパラメーターを定義する
description: 構成依存データのパラメーターを定義する
ms.assetid: a5bb2e3a-22e0-41d7-8035-5437ac473b21
keywords:
- GDL WDK の構成
- WDK GDL、構成に依存するデータの定義の構成
- WDK GDL 構成に依存するデータを定義します。
- PICKMANY パラメーター WDK GDL
- PICKONE パラメーター WDK GDL
- null 値のパラメーター WDK GDL
ms.date: 04/20/2017
ms.localizationpriority: medium
ms.openlocfilehash: aa01a83fcfbe0e3da71bb46b010c9ad1c2b0ad22
ms.sourcegitcommit: a33b7978e22d5bb9f65ca7056f955319049a2e4c
ms.translationtype: MT
ms.contentlocale: ja-JP
ms.lasthandoff: 01/31/2019
ms.locfileid: "56578353"
---
# <a name="defining-the-configuration-dependent-data-parameters"></a>構成依存データのパラメーターを定義する


使用してパラメーターが導入された、 **\*機能**を構築します。 構成体タグ、 **\*機能**コンス トラクターのパラメーターを識別する (とも呼ばれますパラメーター名を定義します。 または、*機能名*)。

コンテンツ、 **\*機能**コンス トラクターは、1 つまたは複数で構成できます**\*オプション**を構築します。 **\*オプション**コンストラクトは許可されている値またはにパラメーターを指定できる状態を定義します。 構成体タグ、 **\*オプション**コンストラクトは許可されている値または状態を識別します。 この構成体タグとも呼ばれますが、*オプション名*します。

たとえば、という名前のパラメーターを定義できます*今日*として次のコード例は、その値として曜日を受け取ることができます。

```cpp
*Feature: Today
{
  *Option: Sunday{}
  *Option: Monday{}
  *Option: Tuesday{}
  *Option: Wednesday{}
  *Option: Thursday{}
  *Option: Friday{}
  *Option: Saturday{}
}
```

上記の例では、*今日*パラメーターは、任意の時点で 1 つの値を取ることができます。 *今すぐ*両方に設定することはできません**日曜日**と**火曜日**します。 ただし、すべてのパラメーターが排他値に制限されています一度に 1 つまたは複数の値になります。 たとえば、同時にそれを手でペンの色を 1 つ以上が保持できるロボットがあれば、定義できますを*PenColors*パラメーターは、現在その手の形の色を記述します。 指定した可能性があります**PenColors:(赤、緑、黄色)** まったく有効な可能性もあります。

 **\*UIType**予約済みディレクティブでは、いつでも (PICKONE) の 1 つの値または特定の時点 (PICKMANY) で複数の値をそのパラメーターに割り当てするかどうかのみをパラメーターが受け取るかどうかを指定することができます。  **\*UIType**の子エントリとしてディレクティブが配置されている、 **\*機能**を構築します。

**注**   "nothing"パラメーターに割り当てられるとも GDL が許可されません。 そのため、ペンを保持していないロボットを記述するには、None と呼ばれるオプションの宣言または PICKMANY パラメーターのオフする必要があります。 使用されるオプション名は重要です。オプションを使用して、このプロパティが割り当てられているを指定することができます、  **\*NoneOption**ディレクティブ。 オプションを **\*NoneOption**指定の他のオプションと互換性がありません。

 

数を定義できます**\*機能**パラメーターがあるように構築します。 すべて**\*機能**にコンストラクトが存在する必要があります、*ルート コンテキスト*します。 ルート コンテキストには、親の構築がありません。

 

 




