---
title: ［メッセージの種類］
description: ［メッセージの種類］
ms.assetid: 3C64F85F-D8AE-4448-A75C-965DCCD85216
keywords:
- NFC
- 近距離無線通信
- proximity
- 近距離近接通信
- NFP
ms.date: 04/20/2017
ms.localizationpriority: medium
ms.openlocfilehash: f4a87ddfd2597b1df7a3f23062f57b4783a35b7b
ms.sourcegitcommit: 0cc5051945559a242d941a6f2799d161d8eba2a7
ms.translationtype: MT
ms.contentlocale: ja-JP
ms.lasthandoff: 04/23/2019
ms.locfileid: "63378683"
---
# <a name="message-type"></a>［メッセージの種類］


メッセージの種類は、ドットで区切られた null で終わる文字列の内容、意味、およびメッセージの目的のコンシューマーを定義します。 例を示します。

``` syntax
    Windows.contoso.com:t
```

メッセージの種類は、プロトコルのコンポーネントで常に開始する必要がありより具体的なサブタイプ情報を続けています。 NFP プロバイダーの観点からは、プロトコル コンポーネントのみを解析およびプロバイダーによって評価される必要があります。 残りの部分は不透明にすることができの公開されたメッセージに一致するサブスクライバーでのみ使用します。

## <a name="protocol-component"></a>プロトコルのコンポーネント


メッセージの種類のプロトコルのコンポーネントは、ドットが含まれていない場合、メッセージの種類、または文字列全体で最初のピリオドの前にすべてのものとして定義されます。 たとえば、強調表示された部分**Windows**。 contoso.com:t プロトコル コンポーネントを示しています。

プロトコル コンポーネントは、プロバイダーのメッセージ処理の分岐のプライマリ レベルを定義する標準化された一意の文字列です。 この文字列では、大文字と小文字を区別します。 パラメーターが NFP プロバイダーへの内容を示します*messageData*はどのようなプロトコル メッセージを送信するときに使用するとします。

ための送信または NFC 経由で受信したメッセージは"NDEF"プロトコルを使用して、 *messageData*既に特定 NDEF 標準に従って書式設定されると仮定されます。

すべての NFP プロバイダーが、"Windows"プロトコルをサポートするために必要です。 "Windows"を指定すると、 *messageData*は、Windows をデコードし、メッセージを理解するために書式設定します。 ただし、 *messageData* NFP テクノロジ基準にはエンコードされません。 NFP プロバイダーは、Windows デバイスで受信者に送信できるように、適切なプロトコル ヘッダー内のメッセージ パラメーターをラップする必要があります。

プロトコルのコンポーネントは、NFP プロバイダーが理解し、サポートしている標準的な型に対応します。 NFP プロバイダーは、呼び出し元は、ドライバーが予期しないプロトコルを指定する場合、エラーを返す必要があります。 これには、メッセージが予期されたプロトコルをエンコードいない理由について、呼び出し元の間で混乱が回避できます。

プロトコルのコンポーネントの最大長は、(ドットまたは終端の NULL は含まれません)、250 文字です。

## <a name="subtype-component"></a>サブタイプ コンポーネント


コンポーネントは、サブタイプ*messageType*最初のドットの後のすべてのものとして定義されます。 たとえば、Windows の強調表示された部分<strong>. contoso.com:t</strong>サブタイプ コンポーネントを示しています。

サブタイプ コンポーネントは、Windows またはアプリケーションによって定義可能性があります。 NFP プロバイダーを使用すると、指定のサブスクライバーにのみメッセージを配信する使用する必要があります*messageType*します。

サブタイプ文字列は大文字小文字を区別し、メッセージの内容について説明します。

プロバイダー、する必要があります。 少なくとも、サポートに次の文字公開された型の場合に、次の文字の使用を制限することをお勧めします。

-   英数字。

    `[A-Za-z0-9]`

-   加えて、次の英数字以外の文字です。

    `- . _~ : / ? # [ ] @ ! $ & ‘ ( ) * + , ; = %`

"Windows"プロトコル、サブタイプの文字列は、URI 名前付けスキームに従う必要があります。 これにより、同じ型を使用して構成が終了しない誤ってを確保しながらしない種類を個別に作成する 2 つの異なる実装できます。

サブタイプ コンポーネントの最大の長さ (終端の NULL は含まない)、250 文字です。 ただし、短い型名が短いメッセージの一般的な近接テクノロジ性質があるためお勧めします。

 

 
## <a name="related-topics"></a>関連トピック
[NFC のデバイス ドライバー インターフェイス (DDI) の概要](https://msdn.microsoft.com/library/windows/hardware/mt715815)  
[フィールドの近接 DDI 参照の近く](https://msdn.microsoft.com/library/windows/hardware/jj866056)  

